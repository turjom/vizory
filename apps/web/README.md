# Vizory marketing site

Static landing page (`index.html`, `styles.css`, `assets/`).

```bash
yarn marketing:dev      # http://localhost:5173
yarn marketing:build    # copies files into dist/
```

Deploy `apps/web` on Vercel (output directory: `dist`).
