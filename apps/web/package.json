{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir dist && cp index.html styles.css robots.txt sitemap.xml dist/ && cp -R assets dist/",
    "preview": "python3 -m http.server 4173 --directory dist",
    "lint": "node -e \"process.exit(0)\"",
    "lint:check": "node -e \"process.exit(0)\"",
    "typecheck": "node -e \"process.exit(0)\""
  }
}
